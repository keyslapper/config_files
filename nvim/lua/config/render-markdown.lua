require("render-markdown").setup({
    html = { enabled = false, },
    latex = { enabled = false, },
    yaml = { enabled = false, },
    pipe_table = {
      enabled = false,
      style = 'none',
    },
})

